# Model Library

## create virtualenv

`uv venv`

## activate virtualenv

`source .venv/bin/activate`

## sync uv

`uv sync`

## To run:

`uv run python -m src.model_library.inference_model_lib --image="data/images/boat.png" --model="yolov8"`